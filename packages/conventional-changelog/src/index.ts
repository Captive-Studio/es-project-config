import defaultConfig from '@w5s/conventional-changelog';

const config: typeof defaultConfig = {
  ...defaultConfig,
  // Override here
};

// eslint-disable-next-line import/no-default-export
export default config;
